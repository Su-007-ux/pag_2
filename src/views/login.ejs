<%- include('partials/header') %>
<div class="container mt-5" style="max-width: 400px;">
<div class="container d-flex justify-content-center align-items-center vh-100">
  <div class="card shadow p-4" style="max-width: 400px; width: 100%;">
    <h3 class="text-center mb-4"><%= t('register_admin') || 'Registro de administrador' %></h3>sign-in-alt"></i> <%= t('login') %></h2>
      <% if (typeof error !== 'undefined') { %>
    <% if (typeof error !== 'undefined') { %>rror %></div>
      <div class="alert alert-danger"><%= error %></div>
    <% } %> action="/login" method="post" autocomplete="off">
        <div class="mb-3">
    <form action="/register" method="POST">-label"><%= t('username') || 'Usuario' %></label>
      <div class="mb-3">ext" class="form-control" id="username" name="username" placeholder="<%= t('username') || 'Tu usuario' %>" required>
        <label for="username" class="form-label"><%= t('username') || 'Usuario' %></label>
        <input type="text" class="form-control" name="username" id="username" required>
      </div>abel for="password" class="form-label"><%= t('password') || 'Contraseña' %></label>
      <div class="mb-3">assword" class="form-control" id="password" name="password" placeholder="<%= t('password') || 'Tu contraseña' %>" required>
        <label for="password" class="form-label"><%= t('password') || 'Contraseña' %></label>
        <input type="password" class="form-control" name="password" id="password" required>
      </div>>
      <button type="submit" class="btn btn-success w-100"><%= t('register') %></button>
    </form>href="/register"><%= t('no_account_register') || '¿No tienes cuenta? Regístrate' %></a>
      </div>
    <div class="text-center mt-3">
      <a href="/login"><%= t('already_account_login') || '¿Ya tienes cuenta? Inicia sesión' %></a>
    </div> href="/auth/google" class="btn btn-danger w-100">
  </div>  <i class="fab fa-google"></i> <%= t('login_with_google') || 'Iniciar sesión con Google' %>
</div>  </a>
      </div>
<%- include('partials/footer') %>
  </div>
</div>
<%- include('partials/footer') %>