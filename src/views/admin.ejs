<!--
  Vista de administración para mostrar la lista de contactos recibidos.

  - Utiliza Bootstrap para el estilo de la tabla y la página.
  - Muestra un encabezado y una tabla con los datos de cada contacto.
  - Los datos se reciben en la variable 'contacts', que es un arreglo de objetos con las propiedades:
    name, email, message, ip, date, country.
  - Incluye Google Analytics mediante el script gtag.js.
-->

<!DOCTYPE html>
<html lang="es">
<head>
  <!-- Google tag (gtag.js) para analítica -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-NMP580DL0H"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-NMP580DL0H');
  </script>
  <meta charset="UTF-8">
  <title>Contactos Recibidos</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">
  <h1 class="mb-4">Lista de Contactos</h1>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Email</th>
        <th>Mensaje</th>
        <th>IP</th>
        <th>Fecha</th>
        <th>País</th>
      </tr>
    </thead>
    <tbody>
      <% contacts.forEach(contact => { %>
        <tr>
          <td><%= contact.name %></td>
          <td><%= contact.email %></td>
          <td><%= contact.message %></td>
          <td><%= contact.ip %></td>
          <td><%= contact.date %></td>
          <td><%= contact.country %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>